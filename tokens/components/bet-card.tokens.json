{
  "component": {
    "betCard": {
      "$description": "The betslip selection card — one bet (event + market + outcome + odds), the core repeating block of the betslip. Two densities share one anatomy: `compact` (Combo/System — odds sits inline to the right of the outcome, no stake field) and `amount` (Single — a Bet-amount field to the right of the outcome column). Header and market are identical across both; the outcome differs (see outcome). Sits directly on the page (surface.page), one elevation up (surface.raised = surface-4), NO border — the surface step alone separates it, unlike Card's hairline. Radius/padding/rhythm per Figma spec (radius.md, 8px padding, 8/4 vertical rhythm). Odds movement colours (up/down/changed) and the LIVE/FB/BB badges and suspended/error states are deliberately OUT of this first pass — they become the separate Odds component + Badge variants + card states.",
      "radius": { "$type": "dimension", "$value": "{radius.md}", "$description": "12px — a touch rounder than the 8px go-to (Card/Button), matching the betslip cards' softer corner." },
      "bg": { "$type": "color", "$value": "{surface.raised}", "$description": "surface-4 — one elevation above the page (surface.page); no border, the surface step alone lifts the card." },
      "padding": { "$type": "dimension", "$value": "{spacing.2}", "$description": "8px inner padding (Figma spec) — 20px sport icon / × sit flush 8px from the card edges." },
      "sectionGap": { "$type": "dimension", "$value": "{spacing.2}", "$description": "8px — header ↔ body block." },
      "lineGap": { "$type": "dimension", "$value": "{spacing.1}", "$description": "4px — between market ↔ outcome ↔ odds lines." },
      "header": {
        "gap": { "$type": "dimension", "$value": "{spacing.2}", "$description": "8px between the leading icon, event link and remove ×." },
        "icon": {
          "size": { "$type": "dimension", "$value": "{spacing.5}", "$description": "20px discipline (sport) glyph." },
          "color": { "$type": "color", "$value": "{icon.secondary}", "$description": "Muted grey — chrome icon, not content." }
        },
        "event": {
          "type": { "$type": "typography", "$value": "{text-style.link-sm}", "$description": "12px underlined link — the event is tappable (opens the event page); truncates with ellipsis." },
          "color": { "$type": "color", "$value": "{text.secondary}", "$description": "Secondary grey; the outcome below is the emphasised line, not the event name." },
          "hoverColor": { "$type": "color", "$value": "{text.active}", "$description": "Turns the active colour on hover — the link is a tap target (opens the event)." }
        },
        "remove": {
          "box": { "$type": "dimension", "$value": "{spacing.5}", "$description": "20px — the × is a ghost icon button, same 20px footprint as the sport icon, flush 8px from the card edge." },
          "iconSize": { "$type": "dimension", "$value": "{spacing.5}", "$description": "20px × glyph (ui/close)." },
          "radius": { "$type": "dimension", "$value": "{radius.sm}", "$description": "6px — subtle rounding on the hover square." },
          "color": { "$type": "color", "$value": "{icon.secondary}" },
          "hoverFill": { "$type": "color", "$value": "{lighten.2}", "$description": "12% white lighten on hover — the ghost-button hover overlay for a small icon button on an elevated surface." },
          "hoverColor": { "$type": "color", "$value": "{text.default}" }
        }
      },
      "market": {
        "type": { "$type": "typography", "$value": "{text-style.body-sm}", "$description": "12px regular — the market name (e.g. 'Match Winner'), muted; truncates." },
        "color": { "$type": "color", "$value": "{text.secondary}" },
        "gap": { "$type": "dimension", "$value": "{spacing.2}", "$description": "8px between the settlement-rules info icon and the market name." },
        "info": {
          "size": { "$type": "dimension", "$value": "{spacing.4}", "$description": "16px settlement-rules info glyph (ui/info-outline)." },
          "color": { "$type": "color", "$value": "{icon.warning}", "$description": "Gold/warning — flags a market with special settlement rules; tapping opens the rich tooltip. Plain markets have no info icon." }
        }
      },
      "outcome": {
        "$description": "The selected outcome. Density-specific weight: in `compact` (Combo/System) it is the emphasised line — 14px semibold (heading-base); in `amount` (Single) the odds is the emphasised value instead, so the outcome steps down to 12px regular (body-sm) but stays white (text.default), not muted.",
        "type": { "$type": "typography", "$value": "{text-style.heading-base}", "$description": "14px semibold (heading-base) — Combo/System." },
        "singleType": { "$type": "typography", "$value": "{text-style.body-sm}", "$description": "12px regular (body-sm), white — Single density, where the odds carries the emphasis." },
        "color": { "$type": "color", "$value": "{text.default}" }
      },
      "odds": {
        "type": { "$type": "typography", "$value": "{text-style.heading-base}", "$description": "14px semibold (heading-base), tabular-nums — the odds value, in both densities. This first pass is the static value only; movement (up/down/changed, struck-through old value) becomes the separate Odds component and recolours to text.positive/text.negative there." },
        "color": { "$type": "color", "$value": "{text.default}" }
      },
      "amount": {
        "$description": "The per-selection Bet-amount field (Single/`amount` density only). A fixed-height floating-label well inset in the card, darker than the card (surface.page) so it reads as an input, not another card. It is a fixed 48px tall and bottom-aligned to the outcome column — deliberately NOT stretched to match the market+outcome+odds height. The currency symbol is a permanent prefix (not typed into the value), and the value is the same semibold as the odds. The ticket-notched silhouette from Figma is deferred to the Input component's bet-amount variant — this pass uses a clean rounded well.",
        "bg": { "$type": "color", "$value": "{surface.page}", "$description": "Darker than the card — an inset input well on surface.card." },
        "border": { "$type": "color", "$value": "{outline.default}" },
        "focusBorder": { "$type": "color", "$value": "{outline.active}", "$description": "Active-colour border on focus — same focus cue as every field." },
        "radius": { "$type": "dimension", "$value": "{radius.md}", "$description": "12px — matches the card's own corner." },
        "width": { "$type": "dimension", "$value": "{spacing.40}", "$description": "160px fixed — a column of Single cards lines up." },
        "height": { "$type": "dimension", "$value": "{spacing.12}", "$description": "48px fixed — a standard field height; bottom-aligned in the card, not stretched to the content height." },
        "paddingX": { "$type": "dimension", "$value": "{spacing.3}", "$description": "12px horizontal." },
        "labelType": { "$type": "typography", "$value": "{text-style.body-xs}", "$description": "10px — the 'Bet amount' floating label." },
        "labelColor": { "$type": "color", "$value": "{text.secondary}" },
        "valueType": { "$type": "typography", "$value": "{text-style.heading-md}", "$description": "16px semibold (heading-md, same as odds), tabular-nums — the entered stake." },
        "valueColor": { "$type": "color", "$value": "{text.default}" },
        "currencyColor": { "$type": "color", "$value": "{text.secondary}", "$description": "The permanent '$' prefix — muted, so it reads as a unit, not part of the number." }
      }
    }
  }
}
