{
  "component": {
    "switch": {
      "$description": "Single size only, same rationale as Checkbox/Radio — a fixed-size control, not a field grid. Native <input type=\"checkbox\"> stays in the DOM (visually hidden, clip-rect technique) so keyboard/forms/screen-readers work for free — only the paired track+thumb are repainted via :checked/:disabled/:focus-visible on the real input, same pattern as Checkbox/Radio. Unlike Checkbox/Radio, the track is always filled (never transparent/outlined) at rest — every reference checked (Radix, MUI, Ant, iOS) draws an off switch as a filled gray pill, not a hollow one, since the track itself is the whole affordance. No indeterminate state — that's a Checkbox-specific tri-state concept with no switch equivalent. Thumb is a flat white circle, no shadow — consistent with the rest of this system having no shadow/elevation token; contrast against the track color alone provides definition, same flat aesthetic as everything else.",
      "radius": { "$type": "dimension", "$value": "{radius.full}" },
      "size": {
        "trackWidth": { "$type": "dimension", "$value": "{spacing.9}", "$description": "36px." },
        "trackHeight": { "$type": "dimension", "$value": "{spacing.5}", "$description": "20px — same footprint as Checkbox's box / Radio's circle, so all three line up when used near each other." },
        "thumb": { "$type": "dimension", "$value": "{spacing.4}", "$description": "16px — leaves a 2px inset on every side of the 20px track height." },
        "thumbInset": { "$type": "dimension", "$value": "{spacing.0_5}", "$description": "2px gap between the thumb and the track's own edge, on all sides." },
        "borderWidth": { "$type": "dimension", "$value": "{spacing.0_5}", "$description": "2px, only used for the focus ring's own additive outline — the track itself has no border, it's a solid fill." },
        "gap": { "$type": "dimension", "$value": "{spacing.2}", "$description": "Track-to-label gap, 8px — same as Checkbox/Radio." }
      },
      "label": {
        "$type": "typography",
        "$value": "{text-style.body-base}",
        "$description": "Same 14px normal weight as Checkbox/Radio's label."
      },
      "state": {
        "default": {
          "track": { "$type": "color", "$value": "{surface.raised}" },
          "thumb": { "$type": "color", "$value": "{color.base.contrast}" },
          "label": { "$type": "color", "$value": "{text.default}" },
          "$description": "Off, at rest — border.default as a solid fill (gray.200), not an outline. White thumb sits on the left."
        },
        "hover": {
          "track": { "$type": "color", "$value": "{outline.strong}" },
          "$description": "Off+hover darkens one gray step (border.strong) — unlike Checkbox/Radio's hollow box, the track is already a sizeable filled area at rest, so a plain gray-darken reads clearly here (the 'too weak a cue' problem was specific to a 2px hairline, not a full fill)."
        },
        "focused": {
          "ringColor": { "$type": "color", "$value": "{outline.active}" },
          "ringWidth": { "$type": "dimension", "$value": "{spacing.1}" },
          "ringOffset": { "$type": "dimension", "$value": "{spacing.0_5}" },
          "$description": "Same additive-ring pattern as Button/Checkbox/Radio/Card."
        },
        "checked": {
          "track": { "$type": "color", "$value": "{fill.active}" },
          "thumb": { "$type": "color", "$value": "{color.base.contrast}" },
          "$description": "Track fills fill.primary (blue.500, same brand blue as checked Checkbox/Radio), thumb stays white and slides to the right."
        },
        "checkedHover": {
          "track": { "$type": "color", "$value": "{fill.activeHover}" },
          "$description": "fill.activeHover (blue.600) — same darken-on-hover pair Button primary and Checkbox/Radio's own checked+hover already use."
        },
        "disabled": {
          "track": { "$type": "color", "$value": "{fill.disabled}" },
          "thumb": { "$type": "color", "$value": "{color.base.contrast}" },
          "label": { "$type": "color", "$value": "{text.disabled}" },
          "$description": "Covers disabled in both off and on positions alike — fill.disabled (gray.100) either way, same pair Button secondary/Input/Checkbox already use for their own disabled states. Thumb stays white; only the track loses color."
        }
      }
    }
  }
}
