{
  "component": {
    "avatar": {
      "$description": "Image-first, initials-second, icon-third — same fallback chain Radix/MUI/GitHub all converge on: render the real photo when a src is given and it loads; if there's no photo (or it fails to load), fall back to the person's initials on a color assigned deterministically from their name; if there isn't even a name (e.g. a not-yet-loaded/system user), fall back further to a generic neutral person icon. One shape only — a circle (radius.full) — no square/rounded variant, not asked for. No AvatarGroup (overlapping stack) or presence/status dot either — both are genuinely separate compositions on top of a single Avatar, deferred until a real use case needs them, same discipline as Switch skipping a group variant it wasn't asked for.",
      "radius": { "$type": "dimension", "$value": "{radius.full}" },
      "border": {
        "$type": "color",
        "$value": "{border.default}",
        "$description": "1px hairline — same 'separate with a border, not a shadow' convention as Card/Input/Select/Search. Applies to a photo and the generic no-name fallback, where it's load-bearing (a light-toned photo or the near-white neutral fallback can otherwise blend into surface.page's white). The 8 identity-color fallbacks (component.avatar.fallback.identity) override this to transparent at the CSS layer — same 'transparent' literal pattern Button ghost's unfilled rest state already uses — because a gray hairline read muddy on top of an already-saturated pastel; the color block itself provides enough separation from the page on its own."
      },
      "size": {
        "sm": {
          "diameter": { "$type": "dimension", "$value": "{spacing.8}" },
          "iconSize": { "$type": "dimension", "$value": "{spacing.4}" },
          "initials": {
            "$type": "typography",
            "$value": {
              "fontFamily": "{family.sans}",
              "fontWeight": "{weight.semibold}",
              "fontSize": "{size.sm}",
              "lineHeight": "{leading.none}",
              "letterSpacing": "{tracking.normal}"
            }
          }
        },
        "base": {
          "diameter": { "$type": "dimension", "$value": "{spacing.10}" },
          "iconSize": { "$type": "dimension", "$value": "{spacing.5}" },
          "initials": {
            "$type": "typography",
            "$value": {
              "fontFamily": "{family.sans}",
              "fontWeight": "{weight.semibold}",
              "fontSize": "{size.base}",
              "lineHeight": "{leading.none}",
              "letterSpacing": "{tracking.normal}"
            }
          }
        },
        "lg": {
          "diameter": { "$type": "dimension", "$value": "{spacing.12}" },
          "iconSize": { "$type": "dimension", "$value": "{spacing.6}" },
          "initials": {
            "$type": "typography",
            "$value": {
              "fontFamily": "{family.sans}",
              "fontWeight": "{weight.semibold}",
              "fontSize": "{size.md}",
              "lineHeight": "{leading.none}",
              "letterSpacing": "{tracking.normal}"
            }
          }
        },
        "$description": "32/40/48 — deliberately the exact same grid as Button/Input/Select/Search (dim.8/dim.10/dim.12), not a new independent scale, so an avatar sits at the same height as a button or field next to it in a row (e.g. a user menu trigger, a comment composer's post button). iconSize is 50% of diameter at every step, the same ratio Button's own iconSize/height already uses. initials font isn't a text-style.* alias — same reasoning as Counter's label: existing bold/label styles bake in uppercase+wide tracking meant for words, wrong for a 1-2 letter initial pair — composed directly from typography primitives instead, weight.semibold (not bold — two bare letters at full bold read slightly heavy at this scale)."
      },
      "fallback": {
        "$description": "Two distinct fallback tiers. 'identity' needs a name to derive both the initials and the color from — used whenever the person is known but has no photo (or it failed to load). 'generic' is for when there's no name either — a plain neutral icon, since there's nothing to color or initial by.",
        "generic": {
          "bg": { "$type": "color", "$value": "{surface.sunken}" },
          "icon": { "$type": "color", "$value": "{icon.muted}" }
        },
        "identity": {
          "$description": "8 hue slots aliasing the new avatar.* semantic roles (tokens/semantic/color.tokens.json), same hue order as chart-series for a consistent categorical rhythm across the system. Slot assignment (hash the name to 0-7) is implementation logic, not a token — this just defines what each slot looks like.",
          "blue": { "bg": { "$type": "color", "$value": "{avatar.blue.bg}" }, "text": { "$type": "color", "$value": "{avatar.blue.text}" } },
          "green": { "bg": { "$type": "color", "$value": "{avatar.green.bg}" }, "text": { "$type": "color", "$value": "{avatar.green.text}" } },
          "magenta": { "bg": { "$type": "color", "$value": "{avatar.magenta.bg}" }, "text": { "$type": "color", "$value": "{avatar.magenta.text}" } },
          "amber": { "bg": { "$type": "color", "$value": "{avatar.amber.bg}" }, "text": { "$type": "color", "$value": "{avatar.amber.text}" } },
          "teal": { "bg": { "$type": "color", "$value": "{avatar.teal.bg}" }, "text": { "$type": "color", "$value": "{avatar.teal.text}" } },
          "orange": { "bg": { "$type": "color", "$value": "{avatar.orange.bg}" }, "text": { "$type": "color", "$value": "{avatar.orange.text}" } },
          "violet": { "bg": { "$type": "color", "$value": "{avatar.violet.bg}" }, "text": { "$type": "color", "$value": "{avatar.violet.text}" } },
          "red": { "bg": { "$type": "color", "$value": "{avatar.red.bg}" }, "text": { "$type": "color", "$value": "{avatar.red.text}" } }
        }
      },
      "image": {
        "fit": { "$type": "string", "$value": "cover", "$description": "object-fit: cover — crops to fill the circle instead of letterboxing, standard for every reference avatar implementation checked." }
      }
    }
  }
}
